name: CI

on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout ref
        uses: actions/checkout@v2

      - name: Install Deno
        uses: denoland/setup-deno@v1
        with:
          deno-version: "1.28.3"

      - name: Check formatting
        run: "deno fmt --check"

      - name: Run tests
        run: "deno test -A"
